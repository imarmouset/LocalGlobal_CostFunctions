# LocalGlobal_CostFunctions

This model implements a global cost function mimicking a top down feedback (e.g. SST neurons), as well as local cost functions mimicking bottom up feedback (e.g. PV cells).  
